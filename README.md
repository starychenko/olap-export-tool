# 📊 OLAP Експорт

<div align="center">
  
![OLAP Export Tool](https://img.shields.io/badge/OLAP-Export%20Tool-blue?style=for-the-badge&logo=microsoft-excel)
  
[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg?logo=python&logoColor=white)](https://www.python.org/)
[![Windows](https://img.shields.io/badge/Windows-0078D6?logo=windows&logoColor=white)](https://www.microsoft.com/uk-ua/windows)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

*Інструмент для автоматизованого експорту та форматування даних з OLAP-кубів*
</div>

---

## 📋 Зміст
- [📊 Огляд](#огляд)
- [✨ Особливості](#особливості)
- [📦 Вимоги](#вимоги)
- [🛠️ Налаштування](#налаштування)
- [🚀 Початок роботи](#початок-роботи)
- [⚙️ Розширені налаштування](#розширені-налаштування)
- [🔄 Автоматична підготовка середовища](#автоматична-підготовка-середовища)
- [📂 Структура проекту](#структура-проекту)
- [🔒 Безпека облікових даних](#безпека-облікових-даних)

---

<a name="огляд"></a>
## 📊 Огляд

Інструмент для автоматизованого підключення до OLAP-кубу, виконання DAX-запитів та експорту отриманих даних у формати Excel (XLSX) і CSV з можливістю вибору. Головний скрипт `olap.py` дозволяє зробити процес збору та аналізу даних більш ефективним, економлячи час на рутинних операціях.

<div align="center">
  
  *Експорт, форматування та аналіз даних з OLAP-кубів*
</div>

---

<a name="особливості"></a>
## ✨ Особливості

- 🔐 **Безпечне підключення** до OLAP-кубу з Windows-аутентифікацією або логіном/паролем
- 🔒 **Надійне шифрування** облікових даних з використанням унікального ідентифікатора пристрою
- 📅 **Гнучкий вибір періодів** - виконання DAX-запитів за різними періодами (рік/тиждень)
- 🧵 **Потоковий експорт CSV** при `EXPORT_FORMAT=CSV` — запис без завантаження всіх даних у пам’ять (економія RAM на великих вибірках)
- 🔄 **Інтерактивність** - анімований індикатор прогресу при виконанні запитів
- ⏱️ **Прогнозування часу завершення** - точний розрахунок часу, що залишився до закінчення обробки
- 📊 **Оптимізоване збереження даних** - підтримка форматів XLSX та CSV з налаштовуваними параметрами
- 📁 **Оптимізація розміру файлів** - використання xlsxwriter для компактних Excel-файлів та оптимальне цитування для CSV
- 🛠️ **Обробка спеціальних значень** - коректна обробка NaN/Infinity значень
- 📦 **Пакетна обробка** - можливість обробки декількох тижнів за один запуск
- ⏱️ **Розумне навантаження** - налаштовувана затримка між запитами для зниження навантаження на OLAP
- 📁 **Структурована організація** файлів результатів (по роках та періодах)
- 🌈 **Інформативність** - кольоровий вивід у консоль для зручного відстеження статусу операцій

---

<a name="вимоги"></a>
## 📦 Вимоги

<details open>
<summary><b>Програмне забезпечення</b></summary>
<br>

- 🐍 Python 3.8 або вище
- 💾 Microsoft SQL Server Management Studio (SSMS) або компоненти ADOMD.NET
- 🪟 Windows (для інтеграції з ADOMD.NET)
</details>

<details open>
<summary><b>Бібліотеки Python</b></summary>
<br>

Основні бібліотеки (автоматично встановлюються через `requirements.txt`):
- **pythonnet** >= 3.0.0 - для .NET інтеграції
- **pyadomd** >= 0.1.0 - для роботи з ADOMD.NET
- **pandas** >= 1.3.0 - для обробки даних
- **xlsxwriter** >= 3.1.0 - для ефективного створення Excel-файлів з меншим розміром
- **python-dotenv** >= 0.19.0 - для керування змінними середовища
- **colorama** >= 0.4.4 - для кольорового виводу в консоль
- **numpy** >= 1.21.0 - для числових операцій
- **cryptography** >= 42.0.0 - для шифрування облікових даних

Для встановлення:
```bash
pip install -r requirements.txt
```
</details>

---

<a name="налаштування"></a>
## 🛠️ Налаштування

<details open>
<summary><b>1. Налаштування ADOMD.NET</b></summary>
<br>

Для роботи з OLAP необхідно налаштувати бібліотеку ADOMD.NET. Є два способи:

### Варіант 1: Використання локальної бібліотеки (рекомендовано)

1. **Використання бібліотеки з папки проекту**:
   - В папці `lib` вже знаходиться необхідна бібліотека `Microsoft.AnalysisServices.AdomdClient.dll`
   - Просто встановіть шлях до неї у файлі `.env`:
   ```
   ADOMD_DLL_PATH=./lib
   ```
   - Цей метод не потребує встановлення SSMS і працює на будь-якому комп'ютері

### Варіант 2: Використання системної бібліотеки

1. **Встановлення ADOMD.NET**:
   - Рекомендований спосіб: встановіть Microsoft SQL Server Management Studio (SSMS)
     - [Офіційна сторінка завантаження SSMS](https://learn.microsoft.com/en-us/ssms/download-sql-server-management-studio-ssms)
   - Альтернативний спосіб: встановіть окремо Microsoft Analysis Services ADOMD.NET клієнт

2. **Налаштування шляху**:
   - Знайдіть шлях до файлу `Microsoft.AnalysisServices.AdomdClient.dll`
   - Типовий шлях для SSMS 20:
   ```
   C:\Program Files (x86)\Microsoft SQL Server Management Studio 20\Common7\IDE\CommonExtensions\Microsoft\SSIS\160\BIShared
   ```
   - Вкажіть цей шлях у файлі `.env` (змінна `ADOMD_DLL_PATH`)
   - Під час запуску інструмент валідує шлях і у разі проблем виводить службові попередження з префіксом `[INIT]` (програма не падає на етапі ініціалізації)
</details>

<details open>
<summary><b>2. Налаштування конфігурації</b></summary>
<br>

Створіть файл `.env` в корені проекту на основі `.env.example`:

```ini
# Параметри підключення
OLAP_SERVER=your_server_name_or_ip
OLAP_DATABASE=your_database_name

# Параметри запиту
FILTER_FG1_NAME=your_filter_category

# Періоди запитів
YEAR_WEEK_START=2024-01
YEAR_WEEK_END=2024-10

# Затримка між запитами (у секундах)
QUERY_TIMEOUT=30

# Шлях до бібліотеки ADOMD.NET
ADOMD_DLL_PATH=C:\Path\To\Your\ADOMD\Directory

# Налаштування форматування Excel
EXCEL_HEADER_COLOR=00365E
EXCEL_HEADER_FONT_COLOR=FFFFFF
EXCEL_HEADER_FONT_SIZE=11

# Формат експорту (XLSX, CSV або BOTH для збереження в обох форматах)
EXPORT_FORMAT=XLSX

# Налаштування для CSV файлів
CSV_DELIMITER=;
CSV_ENCODING=utf-8-sig
# Налаштування цитування: 'all', 'minimal', 'nonnumeric'
# 'minimal' - лапки тільки коли необхідно (найменший розмір файлу)
# 'nonnumeric' - лапки тільки для текстових значень
# 'all' - лапки для всіх значень (найбільший розмір файлу)
CSV_QUOTING=minimal
```

</details>

<details open>
<summary><b>3. Налаштування автентифікації</b></summary>
<br>

Інструмент підтримує два методи автентифікації до OLAP-куба:

1. **Windows-автентифікація (SSPI)** - використовується за замовчуванням:
   ```ini
   # Метод автентифікації: SSPI (Windows) або LOGIN (логін/пароль)
   OLAP_AUTH_METHOD=SSPI
   ```
   При цьому методі використовується обліковий запис поточного користувача Windows.

2. **Автентифікація за логіном і паролем**:
   ```ini
   # Метод автентифікації
   OLAP_AUTH_METHOD=LOGIN
   
   # Облікові дані для підключення можна не вказувати в .env
   # Вони будуть запитані при першому запуску та збережені
   
   # Шифрування збережених облікових даних
   OLAP_CREDENTIALS_ENCRYPTED=true
   
   # Шлях до файлу із збереженими обліковими даними
   OLAP_CREDENTIALS_FILE=.credentials
   ```
   Цей метод дозволяє використовувати окремий обліковий запис для доступу до OLAP-куба.

Обидва методи використовують різні бібліотеки для підключення:
- **SSPI** використовує ADOMD.NET для Windows-автентифікації
- **LOGIN** використовує OleDbConnection з System.Data.OleDb для автентифікації за логіном/паролем

</details>

---

<a name="початок-роботи"></a>
## 🚀 Початок роботи

<div style="padding: 15px; background-color: #FFF3CD; border-left: 6px solid #FFE69C; margin-bottom: 15px;">
⚠️ <b>Важливо</b>: Перш ніж починати роботу, переконайтеся, що виконали всі кроки з розділу "Налаштування".
</div>

### Швидкий старт

1. **Підготуйте середовище** (використовуйте скрипт налаштування, як описано в розділі [Автоматична підготовка середовища](#автоматична-підготовка-середовища))

2. **Запустіть скрипт**:
   ```bash
   python olap.py
   ```

3. **Перегляньте результати** в директорії `result/[рік]/[рік]-[тиждень].xlsx` або `result/[рік]/[рік]-[тиждень].csv`

### Очищення збережених облікових даних

Якщо потрібно видалити збережені облікові дані:
```bash
python olap.py clear_credentials
```

### Вибір періодів для обробки

Ви можете задати періоди двома способами:

1. **Через змінні середовища** в файлі `.env`:
   ```
   YEAR_WEEK_START=2024-01
   YEAR_WEEK_END=2024-10
   ```

2. **Безпосередньо в коді** `olap.py`, змінивши змінні `year_num` та `week_nums`:
   ```python
   year_num = 2024
   week_nums = [1, 2, 3]  # Список тижнів для обробки
   ```

### Налаштування формату експорту

Інструмент підтримує декілька форматів експорту даних:

1. **Excel (XLSX)** - стандартний формат для аналізу в Excel:
   ```
   EXPORT_FORMAT=XLSX
   ```

2. **CSV** - формат для сумісності з іншими програмами:
   ```
   EXPORT_FORMAT=CSV
   ```

3. **Обидва формати** - одночасний експорт в XLSX і CSV:
   ```
   EXPORT_FORMAT=BOTH
   ```

Для CSV-файлів можна налаштувати додаткові параметри:
```
CSV_DELIMITER=;
CSV_ENCODING=utf-8-sig
# Налаштування цитування: 'all', 'minimal', 'nonnumeric'
# 'minimal' - лапки тільки коли необхідно (найменший розмір файлу)
# 'nonnumeric' - лапки тільки для текстових значень
# 'all' - лапки для всіх значень (найбільший розмір файлу)
CSV_QUOTING=minimal
```

---

<a name="розширені-налаштування"></a>
## ⚙️ Розширені налаштування

<details>
<summary><b>🔍 Налаштування DAX-запитів</b></summary>
<br>

DAX-запити налаштовуються в файлі `olap.py` у функції `run_dax_query()`. Ви можете модифікувати їх відповідно до вашої структури даних та вимог.

Основні параметри запиту:
- **Рік і тиждень**: визначають період звіту
- **Фільтр категорії**: налаштовується через змінну `FILTER_FG1_NAME` в `.env`

</details>

<details>
<summary><b>📊 Налаштування обробки спеціальних значень</b></summary>
<br>

Інструмент містить вбудовані механізми для коректної обробки спеціальних числових значень:

1. **NaN та Infinity значення** - автоматично обробляються таким чином:
   - При експорті в XLSX такі значення конвертуються в `None` (пусті клітинки)
   - При експорті в CSV ці значення також замінюються на пусті рядки

2. **Кодування CSV**:
   - Стандартне налаштування `utf-8-sig` забезпечує коректне відображення кириличних символів
   - BOM (Byte Order Mark) додається автоматично для правильного відкриття в Excel

3. **Оптимізоване цитування CSV**:
   - Режим `CSV_QUOTING=minimal` використовує лапки тільки коли це необхідно (найефективніший за розміром файлу)
   - Режим `CSV_QUOTING=nonnumeric` додає лапки тільки навколо текстових значень
   - Режим `CSV_QUOTING=all` додає лапки до всіх значень (найбільший розмір файлу)
   - Оптимальне представлення порожніх значень для зменшення розміру файлу

</details>

<details>
<summary><b>⚡ Оптимізація продуктивності</b></summary>
<br>

Для покращення продуктивності при експорті великих наборів даних реалізовані наступні оптимізації:

1. **Налаштування затримки між запитами**:
   - Параметр `QUERY_TIMEOUT` дозволяє контролювати навантаження на OLAP-сервер
   - Зменшує ймовірність помилок при виконанні довгих запитів

2. **Спрощене форматування**:
   - Форматується лише шапка таблиці, що пришвидшує створення файлів
   - Дані зберігаються без додаткового форматування для більшої швидкості

3. **Оптимізоване цитування в CSV-файлах**:
   - Налаштування `CSV_QUOTING=minimal` зменшує розмір файлів до 30-50%
   - Ефективне представлення порожніх значень
   - Збереження повної сумісності з Excel

5. **Потоковий експорт CSV**:
   - Якщо встановлено `EXPORT_FORMAT=CSV`, дані пишуться у файл рядок-за-рядком без створення проміжного DataFrame
   - Це суттєво зменшує споживання пам’яті при великих наборах даних
   - Для режимів `XLSX` і `BOTH` використовується DataFrame для подальшого форматування XLSX

4. **Інтелектуальний трекер часу**:
   - Клас `TimeTracker` для точного відстеження прогресу та прогнозування часу
   - Адаптивне прогнозування з урахуванням вже оброблених елементів
   - Облік часу обробки та часу очікування між запитами окремо

</details>

<details>
<summary><b>🧪 Діагностика та налагодження</b></summary>
<br>

Проект містить вбудовані інструменти для діагностики:

1. **Детальні повідомлення про помилки**:
   - Технічна інформація про помилки підключення
   - Рекомендації щодо усунення типових проблем

2. **Режим відлагодження**:
   - Змінна `DEBUG=true` в `.env` активує розширену діагностичну інформацію
   - Відображення деталей про час виконання запитів
   - Інформація про коефіцієнти безпеки при прогнозуванні

3. **Кольоровий вивід в консоль**:
   - Різні кольори для різних типів повідомлень (інформація, попередження, помилки)
    - Відображення прогресу з детальною статистикою

4. **Повідомлення ініціалізації .NET**:
    - На етапі ініціалізації валідується `ADOMD_DLL_PATH`
    - У разі проблем показуються службові повідомлення з префіксом `[INIT]` (для швидкої діагностики шляху/бібліотек)

</details>

---

<a name="автоматична-підготовка-середовища"></a>
## 🔄 Автоматична підготовка середовища

Проект містить скрипт PowerShell `setup.ps1` для автоматичної підготовки середовища.

### Що робить скрипт:

1. ✅ Перевіряє наявність Python
2. ✅ Створює віртуальне середовище `venv`
3. ✅ Встановлює всі залежності з `requirements.txt`
4. ✅ Створює файл `.env` на основі `.env.example` (якщо потрібно)

### Як запустити:

```powershell
# Запуск скрипту з підвищеними привілеями (якщо потрібно)
PowerShell -ExecutionPolicy Bypass -File setup.ps1
```

або

```powershell
# Запуск з дозволом виконання скриптів для поточної сесії
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\setup.ps1
```

<div style="padding: 15px; background-color: #E6F6E8; border-left: 6px solid #28A745; margin-bottom: 15px;">
💡 <b>Порада</b>: Скрипт підготовки середовища суттєво спрощує налаштування проекту на новому комп'ютері. Використовуйте його для швидкого старту.
</div>

---

<a name="структура-проекту"></a>
## 📂 Структура проекту

```
olap-export-tool/
├── olap.py              # Головний скрипт для підключення до OLAP та експорту даних
├── setup.ps1            # Скрипт для автоматичного налаштування середовища
├── requirements.txt     # Список залежностей Python
├── .env                 # Файл з конфігурацією (не відстежується Git)
├── .credentials         # Файл з зашифрованими обліковими даними (не відстежується Git)
├── .env.example          # Приклад конфігураційного файлу
├── README.md            # Документація проекту
├── lib/                 # Директорія з зовнішніми бібліотеками
│   └── Microsoft.AnalysisServices.AdomdClient.dll # Бібліотека для роботи з OLAP
└── result/              # Директорія для результатів експорту
    └── YYYY/            # Підпапки за роками
        ├── YYYY-WW.xlsx # Експортовані Excel-файли
        └── YYYY-WW.csv  # Експортовані CSV-файли (якщо увімкнено)
```

---

<a name="безпека-облікових-даних"></a>
## 🔒 Безпека облікових даних

Проект реалізує розширену систему безпеки для збереження облікових даних:

### Шифрування на основі унікального ідентифікатора пристрою

1. **Автоматична генерація унікального ідентифікатора**:
   - Використовує комбінацію системних параметрів (ім'я комп'ютера, домен, користувач, системний диск)
   - Забезпечує прив'язку зашифрованих даних до конкретного пристрою

2. **Надійне шифрування**:
   - Використовується бібліотека `cryptography` з алгоритмом Fernet
   - Облікові дані шифруються/розшифровуються автоматично

3. **Безпечне введення**:
   - Пароль ніколи не відображається під час введення (використовується `getpass`)
   - Відсутність збереження паролів у відкритому вигляді

4. **Контроль доступу до файлів**:
   - Автоматична зміна прав доступу до файлу облікових даних
   - Доступ тільки для поточного користувача

### Управління обліковими даними

- **Автоматичне збереження** після успішної авторизації
- **Безпечне видалення** облікових даних за допомогою команди `python olap.py clear_credentials`
- **Автоматичне оновлення** при помилці автентифікації

Ця система забезпечує високий рівень безпеки, дозволяючи зберегти облікові дані для автоматизованого доступу без компрометації безпеки.

---

## 📜 Ліцензія

Цей проект розповсюджується під ліцензією MIT. Детальніше дивіться у файлі [LICENSE](LICENSE).

---

<div align="center">
  
  Розроблено з ❤️ для спрощення роботи з OLAP
  
  © 2024 | [Контакти для підтримки](mailto:y.starychenko@epicentrk.ua)
</div>