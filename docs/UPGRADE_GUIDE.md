# Посібник з оновлення OLAP Export Tool

## Нові функції (v2.0)

Цей посібник описує нові можливості, додані до OLAP Export Tool.

---

## 🎯 Що нового

### 1. CLI Arguments - Параметри командного рядка
Тепер можна задавати всі параметри через командний рядок без редагування .env файлу.

### 2. Автоматичні ковзаючі періоди
7 варіантів автоматичних періодів замість ручного вказування тижнів.

### 3. Стиснення файлів
Автоматичне створення ZIP архівів після експорту.

### 4. Профілі конфігурації
Збереження наборів налаштувань для різних сценаріїв.

### 5. Вбудований планувальник
Автоматичне виконання експортів за розкладом.

---

## 📦 Встановлення залежностей

```bash
pip install -r requirements.txt
```

Нові залежності:
- `PyYAML>=6.0.0` - для профілів
- `schedule>=1.2.0` - для планувальника

---

## 🚀 Приклади використання

### Базове використання CLI

```bash
# Останні 4 тижні у XLSX з ZIP стисненням
python olap.py --last-weeks 4 --format xlsx --compress zip

# Поточний місяць у CSV
python olap.py --current-month --format csv

# Конкретний період
python olap.py --period 2025-01:2025-12 --format both

# З початку року з кастомним фільтром
python olap.py --year-to-date --filter "Побутова техніка" --compress zip

# Останній квартал
python olap.py --last-quarter --format xlsx --timeout 60
```

### Автоматичні періоди

Доступні варіанти:
- `--last-weeks N` - останні N тижнів
- `--current-month` - поточний місяць
- `--last-month` - попередній місяць
- `--current-quarter` - поточний квартал (Q1-Q4)
- `--last-quarter` - попередній квартал
- `--year-to-date` - з початку року до сьогодні
- `--rolling-weeks N` - ковзаючі N тижнів

### Робота з профілями

```bash
# Перегляд доступних профілів
python olap.py --list-profiles

# Використання профілю
python olap.py --profile weekly_sales

# Перевизначення параметрів профілю
python olap.py --profile weekly_sales --format csv --compress none
```

### Планувальник

```bash
# Одноразове налаштування розкладу
python olap.py --profile weekly_sales --schedule "every monday at 09:00"

# Daemon режим (постійна робота)
python olap.py --profile weekly_sales --daemon
```

---

## 📁 Структура профілів

Профілі зберігаються у директорії `profiles/` у форматі YAML.

### Приклад профілю: `profiles/weekly_sales.yaml`

```yaml
name: weekly_sales
description: Щотижневий звіт по продажам електроніки

period:
  type: auto              # auto | manual
  auto_type: last-weeks   # Тип автоматичного періоду
  auto_value: 1           # Параметр (кількість тижнів)

export:
  format: xlsx            # xlsx | csv | both
  compress: zip           # zip | none
  streaming: false        # true | false
  min_format: false       # true | false

filter:
  fg1_name: Споживча електроніка

connection:
  timeout: 30             # секунди
```

### Формати розкладу

Простий формат (рекомендований):
```
"every monday at 09:00"
"every day at 18:00"
"every 1 week"
"every 3 days at 14:30"
```

Cron формат (експериментальний):
```
"0 9 * * 1"     # Кожен понеділок о 9:00
"0 18 * * *"    # Щодня о 18:00
"0 9 1 * *"     # 1-го числа місяця о 9:00
```

---

## 🔧 Налаштування

### Пріоритет конфігурації

1. **CLI аргументи** (найвищий пріоритет)
2. **Профіль** (якщо вказано --profile)
3. **.env файл** (базова конфігурація)
4. **Defaults** (вбудовані значення)

### Створення власного профілю

Створіть YAML файл у директорії `profiles/`:

```yaml
name: my_custom_report
description: Мій власний звіт

period:
  type: manual
  start: "2025-01"
  end: "2025-12"

export:
  format: both
  compress: zip

filter:
  fg1_name: Споживча електроніка

connection:
  timeout: 45
```

---

## ⚙️ Зворотна сумісність

Всі існуючі методи роботи **повністю підтримуються**:

```bash
# Без аргументів - використання .env (як раніше)
python olap.py

# Clear credentials (існуюча команда)
python olap.py clear_credentials
```

Всі параметри з .env залишаються робочими і використовуються за замовчуванням.

---

## 📋 Повний список CLI аргументів

### Періоди (ручне вказування)
- `--period START:END` - діапазон періодів (YYYY-WW:YYYY-WW)
- `--start YYYY-WW` - початковий період
- `--end YYYY-WW` - кінцевий період

### Автоматичні періоди
- `--last-weeks N` - останні N тижнів
- `--current-month` - поточний місяць
- `--last-month` - попередній місяць
- `--current-quarter` - поточний квартал
- `--last-quarter` - попередній квартал
- `--year-to-date` - з початку року
- `--rolling-weeks N` - ковзаючі N тижнів

### Параметри експорту
- `--format {xlsx,csv,both}` - формат експорту
- `--filter "CATEGORY"` - фільтр категорії
- `--timeout SECONDS` - таймаут між запитами
- `--compress {zip,none}` - стиснення результатів

### Профілі та планування
- `--profile NAME` - використати профіль
- `--schedule "SPEC"` - налаштувати розклад
- `--daemon` - daemon режим
- `--list-profiles` - показати список профілів

### Додаткові опції
- `--debug` - режим налагодження
- `clear_credentials` - видалити збережені дані

---

## 🎓 Сценарії використання

### Щотижневий автоматичний звіт

1. Створіть профіль `profiles/weekly_auto.yaml`:
```yaml
name: weekly_auto
description: Автоматичний щотижневий звіт

period:
  type: auto
  auto_type: last-weeks
  auto_value: 1

export:
  format: xlsx
  compress: zip

filter:
  fg1_name: Споживча електроніка

schedule:
  enabled: true
  simple: "every monday at 06:00"
```

2. Запустіть daemon:
```bash
python olap.py --profile weekly_auto --daemon
```

### Місячний звіт

```bash
# Вручну - експорт поточного місяця
python olap.py --current-month --format both --compress zip

# Або через профіль
python olap.py --profile monthly_report
```

### Аналіз за рік

```bash
python olap.py --year-to-date --format xlsx --compress zip
```

---

## 🐛 Виправлення проблем

### PyYAML не встановлено
```bash
pip install PyYAML>=6.0.0
```

### schedule не встановлено
```bash
pip install schedule>=1.2.0
```

### Профіль не знайдено
Перевірте наявність файлу у директорії `profiles/`:
```bash
python olap.py --list-profiles
```

### Помилка формату розкладу
Використовуйте простий формат:
```
"every monday at 09:00"
"every day at 18:00"
```

---

## 📊 Структура проекту

```
olap-export-tool/
├── olap.py                    # Точка входу
├── olap_tool/
│   ├── __init__.py
│   ├── cli.py                 # CLI аргументи (НОВИЙ)
│   ├── periods.py             # Автоматичні періоди (НОВИЙ)
│   ├── compression.py         # ZIP стиснення (НОВИЙ)
│   ├── profiles.py            # Профілі (НОВИЙ)
│   ├── scheduler.py           # Планувальник (НОВИЙ)
│   ├── runner.py              # Основна логіка (ОНОВЛЕНО)
│   ├── queries.py             # DAX запити
│   ├── exporter.py            # Експорт даних
│   ├── connection.py          # OLAP підключення
│   ├── auth.py                # Автентифікація
│   ├── security.py            # Шифрування
│   ├── progress.py            # Прогрес бар
│   └── utils.py               # Утиліти
├── profiles/                  # Профілі конфігурації (НОВИЙ)
│   ├── weekly_sales.yaml
│   ├── monthly_report.yaml
│   └── quarterly_analysis.yaml
├── logs/                      # Логи scheduler (НОВИЙ)
├── result/                    # Результати експорту
├── .env                       # Конфігурація
└── requirements.txt           # Залежності (ОНОВЛЕНО)
```

---

## ✅ Що протестувати

1. **Базовий CLI**
   ```bash
   python olap.py --last-weeks 1 --format xlsx --compress zip
   ```

2. **Автоматичні періоди**
   ```bash
   python olap.py --current-month --format csv
   ```

3. **Профілі**
   ```bash
   python olap.py --list-profiles
   python olap.py --profile weekly_sales
   ```

4. **Зворотна сумісність**
   ```bash
   python olap.py  # Має працювати як раніше з .env
   ```

5. **Планувальник** (опційно)
   ```bash
   python olap.py --profile weekly_sales --schedule "every monday at 09:00"
   ```

---

## 📞 Підтримка

Якщо виникли питання або проблеми:
1. Перевірте цей посібник
2. Перегляньте приклади профілів у `profiles/`
3. Використовуйте `--help` для перегляду всіх опцій:
   ```bash
   python olap.py --help
   ```
