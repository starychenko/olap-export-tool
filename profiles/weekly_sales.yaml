name: weekly_sales
description: Щотижневий звіт по продажам електроніки

# Період (profile-only секція)
period:
  type: auto
  auto_type: last-weeks
  auto_value: 4

# Ті ж секції що і в config.yaml — перевизначають його
query:
  filter_fg1_name: Споживча електроніка
  timeout: 3

export:
  format: xlsx
  compress: none

xlsx:
  streaming: true
